{
  "name": "slurpee_pubsub",
  "version": "0.0.1",
  "description": "GCloud function that stores pubsub events into datastore",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brent Kirkland",
  "license": "Apache-2.0",
  "dependencies": {
    "@google-cloud/datastore": "^1.0.0",
    "@google/cloud-debug": "^0.9.2"
  }
}
